import { getOptimizationSuggestions } from '../optimization/suggestions';

describe('FinOps Optimization Tests', () => {
  it('should return actionable optimization recommendations', () => {
    const suggestions = getOptimizationSuggestions();
    // Assert array is returned with estimatedSavings property
  });
});
