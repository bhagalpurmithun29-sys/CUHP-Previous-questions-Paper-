# Budget Alerting Thresholds
budgets:
  - environment: development
    monthlyLimit: 500
    alertThresholds: [50, 80, 100]
    actionAt100: notify-slack
  - environment: production
    monthlyLimit: 5000
    alertThresholds: [80, 90, 100]
    actionAt100: trigger-incident
